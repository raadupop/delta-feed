<!--
  Pointer file — canonical agent context is AGENTS.md.
  Kept so Claude Code's auto-discovery still resolves project context.
  Do not add content here; edit AGENTS.md instead.
-->

See [AGENTS.md](./AGENTS.md) for project conventions, architecture iterations,
document hierarchy, and agent context. That is the canonical file across
agents (Claude Code, Codex, Cursor, Copilot); this file is a pointer so
Claude Code's auto-discovery keeps working.
